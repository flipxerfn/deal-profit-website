name = "deal-profit-website"
# Kept on a date supported by the pinned wrangler runtime (4.80.x bundles workerd up to 2026-04-08).
# This pin exists so local dev runs on Node 20; the Worker only uses long-stable APIs.
compatibility_date = "2026-03-15"
main = "./worker/index.js"

[assets]
directory = "./dist"
binding = "ASSETS"
not_found_handling = "single-page-application"
run_worker_first = [ "/api/*" ]

[vars]
# Override these with your real values:
#   - In the Cloudflare dashboard under Settings -> Variables and Secrets, or
#   - Locally in .dev.vars (gitignored), or
#   - Via `npx wrangler secret put <NAME>` / `npx wrangler deploy --var <NAME>:<value>`.
# DISCORD_BOT_TOKEN, ADMIN_USERNAME and ADMIN_PASSWORD are credentials: keep them out of
# wrangler.toml and set them as secrets in the dashboard / .dev.vars.
DISCORD_CATEGORY_IDS = ""